# 预注册验证方案（Pre-registration）

> 制定时间：2026-08-30。目的：把「探索性发现」与「验证性结论」分开，
> 防止事后挑阈值（multiple comparisons / garden of forking paths）。
> 以下阈值在看到更多数据之前冻结；未来台账数据只按本方案判定。

## 冻结的判定规则

### 1. H1 置信度门控（direction3）

- **信号定义**：`predict_direction_3class` 输出的 看多概率 − 看空概率 > **0.25**
- **判定口径**：信号发出后 24 根 H1（约 1 个交易日）的收盘收益 > 0 记为命中
- **有效性判据**：台账累计 n ≥ 100 时，命中率 ≥ 60% 视为门控有效（历史回测值 72%）；
  命中率 ≤ 50%（≈基线）视为失效，撤下「高置信信号」标注
- **已知风险**：回测期（2025-10~2026-08）为黄金牛市；熊市/震荡市未验证

### 2. H1 × D1 交叉过滤

- **信号定义**：H1 置信度 > 0.25 且 `predict_direction_d1` 的 看多概率 − 看空概率 > **0**
- **判定口径**：同上（24h 收益 > 0）
- **有效性判据**：台账累计 n ≥ 30 时，交叉过滤组命中率应高于未过滤组 ≥ 5 个百分点；
  否则判定 D1 过滤无增量，`predict_direction_d1` 的过滤意见标注失效
- **已知风险**：n=20 时 95% 的初始估计不确定度大（Wilson 95% CI ≈ [76%, 99%]）

### 3. D1 独立信号（已判死，不再验证）

D1 模型独立交易已被随机对照实验否定（详见 README「D1 方向模型」一节）。
无论未来数据如何，不复活该结论，除非提出新的随机对照证据。

## 探索性结果（不作为决策依据）

以下均在 2026-08-30 实验中扫过多个阈值组合后选出，属选择效应高风险区，
仅供未来假设生成，不得引用为有效性证据：

- δ=0.30 门控（回测 8 笔 87.5% 胜率）
- H1>0.25 & D1>0.15（n=18, 94.4%）
- H1>0.25 & D1>0.25（n=10, 90%）
- 各分位数阈值（P75/P80/P85/P90/P95）的回测结果

## 对账纪律

- 台账（data/prediction_ledger.jsonl）每周 `gold-accuracy --days 7` 对账一次
- 漂移检查 `gold-drift` 每周一次；PSI>0.25 特征 > 5 个时重训
- 重训后旧模型自动归档 models/archive/，保留至少 3 个版本供回滚对比
